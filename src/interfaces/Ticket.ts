export interface Ticket {
  tickerId: number;
  seatNumber: string;
  tripName: string;
  departureDate: string;
  departureTime: string;
  departureEndTime: string;
  pickupPoint: string;
  payPonit: string;
  email: string;
  numberphone: number;
  name: string;
  status: string;
}